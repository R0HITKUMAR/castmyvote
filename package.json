{
  "name": "castmyvote",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "www": "npm run dev --prefix www",
    "app": "npm run start --prefix app",
    "server": "npm start --prefix server",
    "start": "concurrently \"npm run server\" \"npm run www\" \"npm run app\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}